# meta_kd
The code folder contains all the scripts for training the meta-knowledge distillation encoder. Users can quickly initiate the encoder training process by following the reference environment and dependencies provided below.
